# 2026-06-13 — End of M3 release session (v0.3.0)

> Session checkpoint — part of the [session journal](../../README.md). Checkpoints live outside `ROADMAP.md` per [ADR-0036](../../../adr/0036-session-journal-extraction.md).

- **Done in this session** — the whole of Milestone 3: items 3.1 (ADR-0016 exception policy at the C/C++ boundary + `Pool::try_allocate` + throwing ctor, PR #29), 3.2 (ADR-0017 `TypedPool<T>`, PR #30), 3.3 (ADR-0018 `PoolAllocator<T>` STL Adapter + `memory_pool_block_size` accessor, PR #32), 3.4 (ADR-0019 read-only free-list diagnostic Iterator + `PBR_MEMORY_POOL_DIAGNOSTICS` gate + three `memory_pool_debug_*` accessors, PR #33), 3.5 (container integration suite for `std::list` / `std::vector` / custom `ForwardList`, PR #34), and 3.6 (this PR — the `v0.3.0` release PR: bump `version.hpp` to `0.3.0`, roll `CHANGELOG.md` `Unreleased` into `[0.3.0] — 2026-06-13`, add `docs/releases/v0.3.0.md`, refresh README badge/status/table).
- **Library state on `master`** — unchanged C ABI and O(1) algorithm from v0.2.0. The C++ surface now ships: `Pool` (move-only RAII, throwing ctor, dual-verb `allocate` / `try_allocate` per ADR-0016), `Pool::make` / `PoolBuilder` (non-throwing construction), `TypedPool<T>` (ADR-0017, header-only, compile-time `block_size`, storage + object-lifetime verb layers), `PoolAllocator<T>` (ADR-0018, header-only STL Adapter with deterministic pool/fallback routing), and `FreeListIterator` / `FreeListView` (ADR-0019, header-only, gated behind `PBR_MEMORY_POOL_DIAGNOSTICS`). New public C accessors: `memory_pool_block_size` and the three gated `memory_pool_debug_*` diagnostics functions. CTest registers six targets — `pool_smoke`, `typed_pool`, `pool_allocator`, `free_list_iterator`, `container_integration`, `spec_6_2_valgrind`. New CMake option `PBR_MEMORY_POOL_ENABLE_DIAGNOSTICS`. `CHANGELOG.md` has a sealed `[0.3.0]` block and a fresh empty `Unreleased`.
- **ADRs accepted to date** — 0001–0015 (see prior checkpoint) plus 0016 (exception policy at the C/C++ boundary), 0017 (TypedPool design), 0018 (STL allocator Adapter), 0019 (free-list diagnostic Iterator). Total: 19.
- **Patterns catalogue** — RAII, Pimpl, Factory Method, Builder, **Adapter** (`PoolAllocator`), and **Iterator** (`FreeListIterator` / `FreeListView`) are Implemented. Strategy, Template Method, Composite, Decorator, Observer remain Planned against Milestones 4–6.
- **Spec Coverage Map state at `v0.3.0`** — unchanged from v0.2.0 (eight ✅, one 🚧 at §6.3, §2.2 / §2.4 / §6.3-concurrent in flight). Milestone 3 is C++-side ergonomics over the already-✅ core; ADR-0016 satisfies §2.2's `std::bad_alloc`-C++ clause but the row stays 🚧 pending dynamic growth (M5).
- **Open issues / follow-ups carried into the next session** — none blocking. After this release PR merges, the agent tags `v0.3.0` from `master` (ADR-0008), pushes the tag, and the maintainer clicks *Publish* on the draft GitHub Release produced by `release.yml`.
- **Resume the next session with**, in order:
  1. **M4.1** — ADR: thread-safety **Strategy** (lock-free CAS vs. mutex vs. per-thread caches) and configuration knob (compile-time macro per spec §2.4).
  2. **M4.2** — ADR + impl: **Template Method** allocation skeleton with hook points for the chosen Strategy.
  3. **M4.3** — implementation behind a compile-time switch; default remains single-threaded so the fast path is preserved (spec §2.4).
  4. **M4.4** — concurrent stress tests; TSan job added to CI.
  5. From M4.5 onward; the milestone closes at `v0.4.0` per M4.6.
