# 2026-06-13 — End of M4 release session (v0.4.0)

> Session checkpoint — part of the [session journal](../../README.md). Checkpoints live outside `ROADMAP.md` per [ADR-0036](../../../adr/0036-session-journal-extraction.md).

- **Done in this session** — the whole of Milestone 4: 4.1 (ADR-0020 thread-safety Strategy decision, PR #36), 4.2 (ADR-0021 Template Method skeleton + `SingleThreadedPolicy`, PR #37), 4.3 (the three policies behind the `PBR_MEMORY_POOL_THREAD_SAFETY` compile-time switch + libatomic link for the LOCKFREE DWCAS, PR #38), 4.4 (concurrent stress tests + `tsan` CI job, PR #39), 4.5 (concurrent comparative benchmark + report, PR #40), and 4.6 (this release PR — bump to `0.4.0`, roll `CHANGELOG.md` into `[0.4.0] — 2026-06-13`, add `docs/releases/v0.4.0.md`, refresh README).
- **Library state on `master`** — the v0.3.0 C++ surface, plus compile-time-configurable thread safety. `memory_pool_alloc` / `_free` are an `alloc_skeleton` / `free_skeleton` Template Method delegating to one of three compile-time policies (`SingleThreadedPolicy` / `MutexPolicy` / `LockFreePolicy`) selected by `PBR_MEMORY_POOL_THREAD_SAFETY` (CMake option → PRIVATE macro; default NONE = the v0.3.0 fast path, byte-identical). `struct memory_pool` carries policy state conditionally (atomic tagged head under LOCKFREE, `std::mutex` under MUTEX) within the ADR-0015 128-byte budget. CTest registers seven binaries (the six from M3 + `concurrency_stress`). CI gains `thread-safety` (MUTEX+LOCKFREE × gcc+clang), `tsan` (MUTEX), and `bench-concurrent-smoke` (MUTEX+LOCKFREE) jobs. `CHANGELOG.md` has a sealed `[0.4.0]` block and a fresh empty `Unreleased`.
- **ADRs accepted to date** — 0001–0019 (see prior checkpoints) plus 0020 (thread-safety Strategy + compile-time knob) and 0021 (Template Method allocation skeleton). Total: 21.
- **Patterns catalogue** — RAII, Pimpl, Factory Method, Builder, Adapter, Iterator, **Strategy**, and **Template Method** are Implemented. Composite, Decorator, Observer remain Planned against Milestones 5–6.
- **Spec Coverage Map state at `v0.4.0`** — ten rows ✅ (the eight from v0.2.0 plus §2.4 and §6.3). Remaining ⏳: §2.2's dynamic-growth half (Milestone 5) and the per-block instrumentation items (Milestone 6).
- **Open issues / follow-ups carried into the next session** — none blocking. Per-thread caches are deferred (ADR-0020 §4); LOCKFREE is excluded from TSan by design (benign Treiber-stack races, documented). After this release PR merges, the agent tags `v0.4.0` from `master` (ADR-0008) and the maintainer clicks *Publish*.
- **Resume the next session with**, in order:
  1. **M5.1** — ADR: growth policy (geometric vs. linear) and chunk-linking strategy.
  2. **M5.2** — ADR + impl: **Composite** chunk-list representation linking the original pool with overflow chunks.
  3. **M5.3** — implementation behind a runtime / compile-time flag; default remains fixed-size (spec §2.2).
  4. **M5.4** — tests and benchmarks covering exhaustion-and-grow scenarios.
  5. **M5.5** — close Milestone 5 → `v0.5.0`.
