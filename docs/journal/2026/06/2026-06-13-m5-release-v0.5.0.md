# 2026-06-13 — End of M5 release session (v0.5.0)

> Session checkpoint — part of the [session journal](../../README.md). Checkpoints live outside `ROADMAP.md` per [ADR-0036](../../../adr/0036-session-journal-extraction.md).

- **Done in this session** — the whole of Milestone 5: 5.1 (ADR-0022 dynamic-growth policy decision, PR #42), 5.2 (ADR-0023 Composite chunk-list representation, dormant, PR #43), 5.3 (ADR-0024 growth implementation + `memory_pool_create_dynamic` + lock-free deferral + budget 128→192, PR #44), 5.4 (exhaustion-and-grow tests + growth benchmark, PR #45), and 5.5 (this release PR — bump to `0.5.0`, roll `CHANGELOG.md` into `[0.5.0] — 2026-06-13`, add `docs/releases/v0.5.0.md`, refresh README).
- **Library state on `master`** — the v0.4.0 surface plus optional dynamic growth. The pool is a **Composite**: inline first chunk + an append-only overflow `Chunk` list under one shared free list. `memory_pool_create_dynamic(block_size, block_count, growth_factor)` / `Pool::make_dynamic` / `PoolBuilder::with_growth_factor` create growable pools (default fixed via `memory_pool_create`); growth is geometric, runs inside `pop_head` under the policy's synchronization (NONE / MUTEX), and **lock-free + dynamic is rejected at creation** (ADR-0024 §2). `struct memory_pool` carries `overflow_` + `grow_factor_`; the ADR-0015 budget is 192. CTest registers eight binaries (the seven from M4 + `dynamic_growth`). CI gains the `growth` bench scenario; `bench-concurrent-smoke` is broadened to `bench-policy-smoke` (`--scenario all` under MUTEX + LOCKFREE). `CHANGELOG.md` has a sealed `[0.5.0]` block and a fresh empty `Unreleased`.
- **ADRs accepted to date** — 0001–0021 (see prior checkpoints) plus 0022 (dynamic-growth policy), 0023 (Composite representation), 0024 (growth synchronization + creation surface + lock-free deferral). Total: 24.
- **Patterns catalogue** — RAII, Pimpl, Factory Method, Builder, Adapter, Iterator, Strategy, Template Method, and **Composite** are Implemented. Decorator and Observer remain Planned against Milestone 6.
- **Spec Coverage Map state at `v0.5.0`** — eleven rows ✅ (the ten from v0.4.0 plus §2.2, whose dynamic-growth clause M5 completed). No spec row remains for Milestone 6 (observability is additive instrumentation, not a spec requirement).
- **Open issues / follow-ups carried into the next session** — none blocking. Lock-free dynamic growth, pool shrink-on-idle, and per-thread caches are all deferred (documented). After this release PR merges, the agent tags `v0.5.0` from `master` (ADR-0008) and the maintainer clicks *Publish*.
- **Resume the next session with**, in order:
  1. **M6.1** — ADR + impl: **Decorator** for an instrumented pool variant (counters, allocation histogram, optional logging).
  2. **M6.2** — ADR + impl: **Observer** for pool-lifecycle events (exhaustion, growth, destruction).
  3. **M6.3** — tests verifying zero-overhead in release builds when instrumentation is disabled.
  4. **M6.4** — close Milestone 6 → `v0.6.0`.
