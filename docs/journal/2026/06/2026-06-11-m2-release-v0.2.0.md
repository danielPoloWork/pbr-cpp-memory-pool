# 2026-06-11 — End of M2 release session (v0.2.0)

> Session checkpoint — part of the [session journal](../../README.md). Checkpoints live outside `ROADMAP.md` per [ADR-0036](../../../adr/0036-session-journal-extraction.md).

- **Done in this session** — Milestone 2 items 2.8 (Valgrind CI gate + spec §6.2 demonstrative test, PR #25), 2.9 (microbenchmark + ADR-0014 + canonical v0.2.0 numbers, PR #26), 2.10 (metadata-overhead budget + ADR-0015 + `memory_pool_metadata_bytes` introspection accessor, PR #27), and 2.11 (this PR — the `v0.2.0` release PR rolling `CHANGELOG.md` `Unreleased` into `[0.2.0] — 2026-06-11`, adding `docs/releases/v0.2.0.md`, bumping `version.hpp` to `0.2.0`, and refreshing the README status block and badge).
- **Library state on `master`** — `pbr_memory_pool` static library + `pbr::memory_pool` alias. The four spec §5 C functions carry real O(1) bodies (no more M1 stubs). `memory_pool_free` enforces the ADR-0012 foreign-pointer / out-of-range / misaligned-pointer policy via an O(1) range + alignment check. The C++ surface ships `Pool` (move-only RAII), `Pool::make` (Factory Method), `PoolBuilder` (fluent Builder), and the metadata-bytes accessor. The `pool_vs_malloc_bench` binary ships measured numbers (11.02 × / 5.35 × / 4.45 × on the maintainer's Skylake reference host). CTest registers two targets — `pool_smoke` (the full doctest suite, ~30 cases) and `spec_6_2_valgrind` (the spec §6.2 demonstrative test). CI workflows: `ci.yml` (build matrix + format + tidy + ANSI C / C99 + zero-external-deps + Valgrind + bench-smoke), `docs.yml` (markdownlint + Lychee + ADR sanity), `release.yml` (tag-triggered artifact + draft-release pipeline). `CHANGELOG.md` has a sealed `[0.2.0]` block and a fresh empty `Unreleased`.
- **ADRs accepted to date** — 0001 (record ADRs), 0002 (cross-language source layout), 0003 (design-patterns policy), 0004 (versioning & release policy), 0005 (toolchain matrix), 0006 (code style + clang-tidy baseline), 0007 (doctest), 0008 (delegate tag creation + push to the agent), 0009 (free-list layout + `block_size` constraints + alignment), 0010 (RAII + Pimpl), 0011 (Factory Method + Builder), 0012 (foreign-pointer policy), 0013 (Doxygen for API + Markdown for narrative), 0014 (microbenchmark methodology), 0015 (metadata-overhead budget). Total: 15.
- **Spec Coverage Map state at `v0.2.0`** — eight rows ✅ (§2.1, §2.3, §3.1, §3.2, §3.3, §4, every §5 function row, §6.1, §6.2), one row 🚧 (§6.3 — single-threaded coverage complete, concurrent re-run at M4.5), three rows ⏳ (§2.2 — the C++-side return policy half + dynamic-growth half, §2.4 — thread safety, §6.3 — the concurrent half). All algorithm-bearing single-threaded contracts are now ✅.
- **Open issues / follow-ups carried into the next session** — none blocking. After this release PR merges, the agent tags `v0.2.0` from `master` (ADR-0008), pushes the tag, and the maintainer clicks *Publish* on the draft GitHub Release produced by `release.yml`.
- **Resume the next session with**, in order:
  1. **M3.1** — ADR: exception policy at the C / C++ boundary — `NULL` on the C side, `std::bad_alloc` on the C++ side, behind a configurable knob (spec §2.2 — *"return `NULL` (or throw an exception in C++)"*).
  2. **M3.2** — `it::d4np::memorypool::TypedPool<T>` template with RAII lifetime and `try_allocate` / `allocate` variants.
  3. **M3.3** — ADR + impl: **Adapter** — STL-compatible allocator over the underlying pool; propagation traits specified in the ADR.
  4. **M3.4** — ADR + impl: **Iterator** (read-only) over the free list for diagnostics — disabled in release builds unless explicitly enabled.
  5. From M3.5 onward as listed in the Milestone 3 section above; the milestone closes at `v0.3.0` per M3.6.
